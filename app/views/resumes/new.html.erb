<div class="col-md-10 col-md-offset-1 job-content">
  <div class="main-box">
   <div class="col-md-10 col-xs-6 job-box ">
     <div class= "text centor">
      <div class= "text centor">
       <h3> 提交简历到 <%= @job.title %> </h3>
      </div>
     <hr>

    <%= simple_form_for [@job, @resume] do |f| %>
    <%= f.input :content  %>
    <%= f.input :attachment %>
    <%= f.submit "送出" , class: "btn btn-info"%>
    <% end %>
   </div>
  </div>
 </div>
